server:
  port: 10086

spring:
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
        prefix: public-config
  application:
    name: blg-demo
  rabbitmq:
    host: localhost
    port: 30120
    username: guest
    password: guest
    virtual-host: demo-virtual